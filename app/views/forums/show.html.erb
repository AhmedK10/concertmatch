<%= simple_form_for [@forum, @comment], html: { class: "d-flex form-group" } do |f| %>
  <%= f.input :content, label: false, placeholder: "Message #{current_user&.first_name}", wrapper_html: { class: "flex-grow-1" } %>
  <%= f.submit "Send", class: "button" %>
<% end %>
