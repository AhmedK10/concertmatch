
<%= simple_form_for [@forum, @comment],
  html: {class: "d-flex"} do |f|
%>
  <%= f.input :content,
    label: false,
    # placeholder: "Message ##{@comment.user.first_name}",
    wrapper_html: {class: "flex-grow-1"}
  %>
  <%= f.submit "Send", class: "button mb-3" %>
<% end %>
